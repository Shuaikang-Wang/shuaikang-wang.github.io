<h2 style="margin-bottom: 2rem;">
  <a style="color: inherit"><b>Awards and Honors</b></a>
</h2>
<ul class="awards-list" style="padding-left: 2em;">
  {% for award in page.awards.items %}
    <li style="margin-bottom: 1em;">
      <span style="font-weight: 500;">{{ award.date }}:</span>
      <b>{{ award.title }}</b>
      {% if award.organization %}
        <span style="font-style: italic">{{ award.organization }}</span>
      {% endif %}
    </li>
  {% endfor %}
</ul>